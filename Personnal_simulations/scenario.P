/* attackerLocated(workStation_1). */
/* attackGoal(netAccess(workStation_1, _, _)). */
attackerLocated(serveur_2).
attackGoal(execCode(serveur_2, root)).

/* hacl(workStation_1, _, _, _).
hacl(serveur_2, _, _, _). */

/* configuration information of workStation_1 */
/* hasAccount(user1, workStation_1, user). */

/* configuration information of dc2 */
setuidProgramInfo(dc2, Sw2, root).
vulExists(dc2, 'CVE-2021-1675', Sw2 ).
vulProperty('CVE-2021-1675', localExploit, privEscalation).
cvss('CVE-2021-1675', h).


/* configuration information of serveur_2 */
networkServiceInfo(serveur_2, vpnService, _, _, _).
setuidProgramInfo(serveur_2, Software, root).
vulExists(serveur_2, 'CVE-2021-42287', Software ).
vulProperty('CVE-2021-42287', localExploit, privEscalation).    
cvss('CVE-2021-42287', h).

/* Les utilisateurs */
hasAccount(user1, serveur_2, user).
inCompetent(user1).
